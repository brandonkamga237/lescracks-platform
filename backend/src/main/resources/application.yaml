spring:
  application:
    name: lescracks
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  config:
    import: optional:classpath:application-${spring.profiles.active}.yml
  main:
    allow-circular-references: true

  # Default datasource configuration (overridden by profile-specific files)
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:-lescracks}
    username: ${DB_USERNAME:-postgres}
    password: ${DB_PASSWORD:-postgres}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 20000
      idle-timeout: 300000
      max-lifetime: 1200000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  security:
    oauth2:
      client:
        registration:
          github:
            client-id: ${GITHUB_CLIENT_ID}
            client-secret: ${GITHUB_CLIENT_SECRET}
            scope: read:user, user:email
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope: profile, email

  mail:
    host: ${MAIL_HOST:mail.privateemail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:contact@lescracks.com}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
        debug: false

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

app:
  jwt:
    secret: ${JWT_SECRET:lescracks-secret-key-minimum-256-bits-long-for-hs256}
    expiration: ${JWT_EXPIRATION:86400000}
  frontend:
    url: ${FRONTEND_URL:http://localhost:5173}
  cors:
    allowed-origins: ${CORS_ORIGINS:http://localhost:5173}

  uploads:
    dir: ${UPLOADS_DIR:uploads/resources}
    avatar-dir: ${AVATAR_DIR:uploads/avatars}

  mail:
    from: ${MAIL_FROM:LesCracks <contact@lescracks.com>}
    admin: ${MAIL_ADMIN:contact@lescracks.com}
    reset-token-expiry-minutes: 30

  # MinIO Configuration
  minio:
    url: http://${MINIO_HOST:localhost}:${MINIO_PORT:9000}
    access-key: ${MINIO_ACCESS_KEY:minioadmin}
    secret-key: ${MINIO_SECRET_KEY:minioadmin}
    bucket: ${MINIO_BUCKET:lescracks}
    public-url: http://${MINIO_HOST:localhost}:${MINIO_PORT:9000}

  # Swagger/OpenAPI Configuration
  swagger:
    enabled: ${SWAGGER_ENABLED:true}
    title: "LesCracks API"
    version: "1.0.0"
    description: "API pour la plateforme de formation LesCracks - Gestion des utilisateurs, événements, ressources et formations"

management:
  health:
    mail:
      enabled: false   # Mail server unreachable doesn't mean the app is down
