package com.brandonkamga.lescracks.domain;

/**
 * Where a resource's substance lives, which is what separates the three.
 *
 * VIDEO   a reference to somewhere else, typically YouTube
 * EBOOK   a file the platform holds
 * ARTICLE text written on the platform
 */
public enum ResourceKind { VIDEO, EBOOK, ARTICLE }
