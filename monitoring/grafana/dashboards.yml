apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: 'LesCracks'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 60
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
